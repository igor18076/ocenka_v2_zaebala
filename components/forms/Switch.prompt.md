# Switch

Binary on/off toggle, emerald when active. Use for settings and "применять подход" toggles.

```jsx
<Switch label="Применять доходный подход" defaultChecked />
<Switch label="Уведомления по e-mail" />
```

Forwards native input attributes (`checked`, `onChange`, `disabled`).
